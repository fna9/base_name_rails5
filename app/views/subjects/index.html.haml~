.page-header
  <h1Дисциплины</h1>

%table.table.table-striped
  %thead
    %tr
      %th Название
      %th Тип
      %th Вид отчетности

  %tbody
    - @subjects.each do |subject|
      %tr
        %td= subject.title
        %td= subject.s_type
        %td= Subject::RTYPES[subject.type_of_reporting]
        %td
          = link_to subject,"data-toggle" => "tooltip", :title => "Показать", class: "btn btn-success btn-xs" do
            =fa_icon "arrow-circle-right"
            Показать

        %td
          = link_to edit_subject_path(subject), class: "btn btn-warning btn-xs" do
            =fa_icon "edit"
            Редактировать

        %td
          = link_to subject, :method => :delete, :data => { :confirm => 'Вы уверены?' },class: "btn btn-danger btn-xs" do
            =fa_icon "remove"
            Удалить

%br

= link_to new_subject_path, "data-toggle" => "tooltip", :title => "Создание новой дисциплины", class: "btn btn-primary" do
  =fa_icon "plus"
  Добавить дисциплину
