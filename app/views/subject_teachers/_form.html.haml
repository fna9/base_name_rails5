= form_for @subject_teacher do |f|
  - if @subject_teacher.errors.any?
    #error_explanation
      %h2= "#{pluralize(@subject_teacher.errors.count, "error")} prohibited this subject_teacher from being saved:"
      %ul
        - @subject_teacher.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :subject
    = f.text_field :subject
  .field
    = f.label :teacher
    = f.text_field :teacher
  .actions
    = f.submit 'Save'
