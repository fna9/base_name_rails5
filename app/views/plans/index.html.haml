.page-header
  <h1>Учебные планы</h1>

%table.table
  %thead
    %tr
      %th Код
      %th Название
      %th Уровень
      %th Форма обучения
      %th Сроки обучения
      %th Учебные группы
  %tbody
    - @plans.each do |plan|
      %tr
        %td= plan.code
        %td= plan.title
        %td= plan.level
        %td= plan.form_of_study
        %td= plan.training_period
        %td= raw(plan.groups.map{ |g| link_to(g.title, group_path(g)) }.join(', '))
        %td
          = link_to plan,"data-toggle" => "tooltip", :title => "Развернутая информация", class: "btn btn-success btn-xs" do
            =fa_icon "arrow-circle-right"
            Показать

        %td
          = link_to edit_plan_path(plan), class: "btn btn-warning btn-xs" do
            =fa_icon "edit"
            Редактировать

        %td
          = link_to plan, :method => :delete, :data => { :confirm => 'Вы уверены?' },class: "btn btn-danger btn-xs" do
            =fa_icon "remove"
            Удалить


%br

= link_to 'Создать новый учебный план', new_plan_path



