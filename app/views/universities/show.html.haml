.br
.page-header
  .h1=@university.full_name +  ' ' + '(' + @university.name + ')'


= link_to edit_university_path(@university), class: "btn btn-warning" do
  =fa_icon "pencil"
  Редактировать
\|
= link_to universities_path, class: "btn btn-primary" do
  =fa_icon "reply"
  Вернуться к списку учебных заведений
