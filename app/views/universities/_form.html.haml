= form_for @university do |f|
  - if @university.errors.any?
    = error_messages_for @university

  .row
    .col-md-8
      .form-group
        *
        =f.label :full_name
        =f.text_field(:full_name, class: 'form-control', :placeholder => "Наименование (полностью)")
        
  .row
    .col-md-8
      .form-group
        *
        =f.label :name
        =f.text_field(:name, class: 'form-control', :placeholder => "Наименование (сокращенное)")
        

  .h5 * - поля обязательные для заполнения!
        
        
  .actions
    = f.button "data-toggle" => "tooltip", class: "btn btn-primary" do
      =fa_icon "check"
      Сохранить
      
 
