= form_for @afile do |f|
  - if @afile.errors.any?
    = error_messages_for @afile
		

  .row
    .col-md-8
      .form-group
        =f.label :text
        =f.text_field(:text, class: 'form-control')
        =f.error_messages_for(:text)

  .actions
    = f.submit 'Сохранить'
